@model SpotifyShuffler.Models.SettingsModel

@{
    Layout = "_Layout";
}

<h3>
    Settings Page.
</h3>

<div class="container">
    <div class="row">
        <div class="col-xl-8 offset-xl-2 col-md-10 offset-md-1 col-xs-12">
            @await Html.PartialAsync("_EmailAddressSettings", Model.CurrentUser.EmailAddress)
        </div>
    </div>
</div>