@model SpotifyShuffler.Models.CreateEmailAddressModel

@{
    Layout = "_Layout";
}

<div class="container m-4">
    <div class="row">
        <div class="col-xs-12 col-xl-8 col-md-10 offset-md-1 offset-xl-2">
            <h1 class="h3">
                Created Email Address
            </h1>
            
            <p>
                Your email address @Model.Email is confirmed.
            </p>

            @Html.ActionLink("Home", "Home", "Home", new {}, new {@class = "btn btn-primary"})
        </div>
    </div>
</div>